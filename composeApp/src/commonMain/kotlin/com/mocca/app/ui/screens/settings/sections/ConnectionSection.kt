package com.mocca.app.ui.screens.settings.sections

/**
 * Settings section: Connection
 * 
 * Connection section has been pruned — all previous toggles (auto reconnect,
 * data saver mode) were removed as dead preference fields.
 */
